
SCHEMA >
    `version` String,
    `id` String,
    `detail_type` String,
    `source` String,
    `account` String,
    `time` String,
    `region` String,
    `operation_id` String,
    `operation_type` String,
    `reason` LowCardinality(String),
    `status_code` String,
    `status_reason` String,
    `status` String,
    `status_description` String,
    `original_value` String,
    `detail_id` String,
    `detail_status` String,
    `timestamp` String,
    `creation_date` String,
    `partner_id` String,
    `plugin_id` String,
    `global_trace_id` String,
    `payment_input_type` String,
    `payment_card_holder_name` String,
    `payment_brand` String,
    `payment_type` String,
    `payment_origin` String,
    `shop_id` String,
    `external_id` String,
    `submerchant_id` String,
    `submerchant_soft_descriptor` String,
    `submerchant_legal_name` String,
    `submerchant_name` String,
    `submerchant_tax_id_type` String,
    `submerchant_mcc` String,
    `submerchant_street_name` String,
    `submerchant_street_number` String,
    `submerchant_address_notes` String,
    `submerchant_zip_code` String,
    `submerchant_city` String,
    `submerchant_province` String,
    `submerchant_country` String,
    `device_id` String,
    `device_ksn` String,
    `device_pin_capacity` String,
    `device_type` String,
    `device_application_version` String,
    `device_pos_condition_code` String,
    `buyer_name` String,
    `buyer_doc_type` String,
    `buyer_doc_number` String,
    `amount_value` String,
    `amount_currency` String,
    `process_code` String,
    `audit_number` String,
    `ticket_number` String,
    `batch_number` String,
    `nii` String,
    `rrn` String,
    `authorization_response` String,
    `code_response` String,
    `terminal_identification` String,
    `custom_field_59` String,
    `system_datetime` String,
    `amount_settlement` String,
    `original_date` String,
    `installment_plan_id` String,
    `installment_plan_acquirer_plan_id` String,
    `installment_plan_installment` String

ENGINE "MergeTree"
ENGINE_SORTING_KEY "detail_id"
